export type  projectType = {
    title: string,
    description: string,
    skills: string[],
    poster?: string,
    videoDemo: string,
    isLive?: boolean,
    isCompleted: boolean,
    url?: string
    sourceCode: string,
}